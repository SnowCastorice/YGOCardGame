# YGO Pack Opener - Python 工具依赖
# 使用方法：local/venv/Scripts/python.exe -m pip install -r tools/requirements.txt
#
# ⚠️ PaddlePaddle GPU 需要单独安装（不在此文件中），因为必须指定飞桨官方源：
#   local/venv/Scripts/python.exe -m pip install paddlepaddle-gpu==3.2.0 \
#     -i https://www.paddlepaddle.org.cn/packages/stable/cu126/
# 详见 docs/SETUP.md 的 OCR 环境章节

# OCR 工具链
paddleocr==3.4.0
opencv-contrib-python
opencv-python-headless

# 数据处理
pandas
openpyxl
requests
PyYAML

# 其他工具
pyperclip
tqdm
